# Copier en .env avant de lancer l'application : cp .env.example .env
#
# URL de base de l'API BookList Pro, sans barre oblique finale.
# Le prefixe EXPO_PUBLIC_ est ce qui rend la variable lisible depuis le code client.
EXPO_PUBLIC_API_URL=http://localhost:3000
