# ADR 005 — Écritures optimistes et concurrence des bascules

## Statut

Accepté — 10/09/2026

## Contexte

Le lot 2 exige des **mises à jour optimistes sur le coup de cœur et sur le statut de lecture, avec retour arrière si le serveur refuse**, et demande de le démontrer en mode dégradé : « un libraire qui clique sur le cœur doit voir l'effet immédiatement, et voir l'annulation si l'écriture échoue ».

Trois faits du terrain pèsent sur la décision.

**La latence est réelle.** En mode chaos l'API répond en 1,5 s avec 40 % de gigue et rejette 30 % des requêtes en 503. Un cœur qui attend la réponse du serveur est un cœur qui ne réagit pas pendant deux secondes, au comptoir, un client devant.

**L'API porte un verrou optimiste.** `PATCH /books/:id` accepte un en-tête `If-Match` et répond 409 avec `{ erreur: "conflit", serveur, versionAttendue }` si la version envoyée n'est plus celle du serveur. Le lot 1 s'en sert déjà pour le `PUT` du formulaire (ADR 002).

**Le tri et les filtres sont côté serveur.** Rien, côté client, ne permet de savoir sur quelle page un ouvrage modifié atterrira. Le lot 1 invalide donc `['books', 'list']` après chaque mutation (ADR 001).

Deux questions en découlent, et les réponses du lot 1 ne s'y transposent pas telles quelles.

## Question 1 — Une bascule doit-elle porter une version ?

### Options envisagées

1. **Envoyer `If-Match` comme pour le `PUT`.** Cohérent en apparence : une seule politique de concurrence pour toutes les écritures. Mais la version détenue par une ligne de liste vieillit dès qu'un collègue corrige un titre depuis une autre boutique. Le libraire clique sur le cœur, reçoit un 409, et n'a rien à arbitrer : il ne voulait pas écrire le titre, il voulait mettre un cœur. Le conflit est signalé sans qu'aucune décision ne soit possible.
2. **Ne pas envoyer de version sur les bascules.** Le serveur applique le dernier écrivain. C'est exactement la sémantique voulue : « marque cet ouvrage comme coup de cœur » ne contredit la correction d'aucun collègue, et deux libraires qui mettent le même cœur veulent la même chose.
3. **Rejouer automatiquement après un 409**, en relisant la version serveur. Deux allers-retours au lieu d'un, sur les connexions les plus mauvaises du réseau, pour un résultat identique à l'option 2 dans tous les cas où le champ modifié n'est pas celui qui a bougé.

### Décision

Option 2. **`PATCH` sur `lu` et `favori` part sans `If-Match`. `PUT` garde le sien.**

La distinction n'est pas un relâchement, c'est la nature de l'écriture :

- **`PUT` envoie la représentation entière.** Enregistrer un formulaire écrase le titre, l'auteur, l'éditeur et l'année tels qu'ils étaient à l'ouverture de l'écran. Là, un conflit est réel : la saisie d'un collègue disparaîtrait. Il doit être détecté et arbitré.
- **`PATCH` sur un booléen exprime une intention isolée.** Aucun autre champ n'est touché, aucune saisie n'est écrasée. Le seul conflit possible serait deux libraires basculant le même champ en sens inverse dans la même seconde — cas où le dernier clic est précisément la bonne réponse.

La règle générale du projet reste donc : **on verrouille ce qui écrase, on ne verrouille pas ce qui ajoute une intention.**

## Question 2 — Que faire des listes après une bascule acceptée ?

L'invalidation du lot 1 (`invalidateQueries(['books', 'list'])`) déclenche un refetch. Sous le filtre « coups de cœur », retirer un cœur ferait disparaître la ligne sous le doigt du libraire, sans recours, moins d'une seconde après son geste.

### Décision

`invalidateQueries({ queryKey: bookKeys.lists(), refetchType: 'none' })`.

Les listes sont **marquées périmées mais pas rechargées**. L'écran affiché garde la ligne, avec son nouvel état ; la prochaine visite lit des données fraîches. Le libraire voit ce qu'il vient de faire, et l'ouvrage sort de la sélection au moment où il y revient — pas pendant qu'il la regarde.

L'écriture optimiste, elle, touche **le détail et toutes les listes en cache**, en préservant l'identité des pages qui ne contiennent pas l'ouvrage : c'est cette identité qui permet à `React.memo` de ne pas redessiner les dix-neuf autres lignes.

## Question 3 — Où vivent les notes de lecture ?

Clé `['books', 'detail', id, 'notes']`, sous la fiche et non à la racine.

La hiérarchie porte l'invalidation, comme au lot 1 : `removeQueries(['books', 'detail', id])` au moment d'une suppression emporte la fiche **et** ses notes. Une clé racine `['notes', id]` laisserait les notes d'un ouvrage supprimé dans le cache, prêtes à réapparaître.

Les notes ne sont pas paginées : la route renvoie un tableau nu, un ouvrage en porte une poignée, et paginer une poignée coûterait un aller-retour pour rien. `pageSchema` reste réservé à la collection.

## Question 4 — Recherche : anti-rebond et annulation

L'anti-rebond de 300 ms vit **dans la barre de recherche** (`components/books/BookToolbar.tsx`), pas dans le hook de données. Le texte saisi est un état local de la barre ; seule la valeur stabilisée remonte. Conséquence directe : **une frappe ne provoque aucun rendu de la liste**, puisque l'écran qui la porte n'apprend rien avant la pause. C'est la preuve demandée par le sujet, et elle est vérifiée par un test qui compte les rendus (`components/__tests__/BookToolbar.test.tsx`).

L'annulation de la requête précédente n'est pas écrite à la main : le `queryFn` consomme le `signal` fourni par TanStack Query, et un changement de clé de requête abandonne le dernier observateur de la requête précédente, ce qui avorte son `fetch`. La réponse à « arc » ne peut donc pas arriver après celle de « archipel ».

## Conséquences

**Positives** : le cœur réagit au clic, y compris en mode dégradé ; un refus est visible et réversible sans que le libraire ait à comprendre ce qui s'est passé ; aucun 409 inutile sur les bascules ; la barre de recherche ne coûte rien à la liste.

**Négatives** : deux politiques de concurrence coexistent et doivent être expliquées, ce que fait cette ADR ; une bascule concurrente est résolue en « dernier arrivé gagne », sans trace.

**À revoir si** : le lot 4 fait entrer les bascules dans la file de mutations persistée. Rejouer une bascule vieille de dix minutes n'a pas la même innocuité qu'un clic immédiat, et il faudra alors décider si la file porte l'intention (« mettre en coup de cœur ») ou l'état observé au moment du clic.
